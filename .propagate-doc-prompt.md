Read the file .propagate-context.md for context information.

After you are done, append any problems in the workflow, like missing information or tools, you encountered to the file
.propagate-problems.md.

API-Schemas are available at $API_URL:/swagger-ui

Test against a local docker instance, it's must faster than testing against the cloud version.

PDFDANCER_API_TOKEN=42

You are not allowed to make any changes to github workflow files.

PDF Examples files are available in the public github repository https://github.com/MenschMachine/pdfdancer-examples.


You are updating capability documentation in the pdfdancer-client-python-examples repository.

The following capability files were propagated from the source repo:
- docs/capabilities/CLEAR_CLIPPING.md

These files describe what the capability does. Your job is to add an
"## Implementation in pdfdancer-client-python-examples" section to each capability file
that explains how this feature was implemented in THIS repo.

Look at the diff below to understand what was changed, then update
each capability file with a concise description of:
- What files were added or changed
- How the feature works in this repo
- Any repo-specific details (endpoints, components, configs, etc.)

Do NOT rewrite existing content in the capability files. Only append
the implementation section.

# Changes made

```diff
diff --git a/README.md b/README.md
index 08f73ce..8e45eca 100644
--- a/README.md
+++ b/README.md
@@ -64,14 +64,18 @@ API docs structure:
   - `02_fill_fields.py` – populate fields by name.
   - `03_check_boxes.py` – toggle checkbox widgets.
   - `04_clear_fields.py` – reset fields to defaults.
+- `examples/clipping/`
+  - `01_clear_clipping.py` – remove clipping so hidden image and path content becomes visible.
 - `examples/working-with-images/`
   - `01_list_images.py` – list image locations.
   - `02_move_image.py` – reposition the first image on page 0.
   - `03_delete_images.py` – remove images from a specific page.
 
-All scripts use `examples/Showcase.pdf` as the input document and write their
-outputs inside `output/<category>/`. Logs produced by the SDK are written to
-`logs/` – useful for inspecting raw requests while you experiment.
+Most scripts use `examples/Showcase.pdf` as the input document and write their
+outputs inside `output/<category>/`. The clipping example uses
+`examples/clipping/invisible-content-clipping-test.pdf`. Logs produced by the
+SDK are written to `logs/` - useful for inspecting raw requests while you
+experiment.
 
 ## Running examples
 
@@ -84,6 +88,7 @@ python examples/quickstart/01_inspect_document.py
 python examples/working-with-text/01_find_and_replace.py
 python examples/working-with-pages/02_extract_pages.py
 python examples/forms/02_fill_fields.py
+python examples/clipping/01_clear_clipping.py
 python examples/working-with-images/02_move_image.py
 ```
 
diff --git a/requirements.txt b/requirements.txt
index 0944375..f96dc0e 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -1 +1 @@
-pdfdancer-client-python==0.3.11
+git+https://github.com/MenschMachine/pdfdancer-client-python.git@2e92b4e1822e170848f210b3c90d679b59983ebc

```