Page management examples that align with the "Working with Pages" docs. All
scripts start from `examples/Showcase.pdf` and write results to
`output/working-with-pages/`.

- `01_reorder_pages.py` – move a page to a new index.
- `02_extract_pages.py` – keep the first N pages and discard the rest.
- `03_delete_pages.py` – delete a specific page by index.
- `04_add_blank_page.py` – append a blank Letter-sized page.
