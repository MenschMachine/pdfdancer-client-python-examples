Clipping workflows for PDFs where content exists but is hidden by a clipping
path. Scripts in this directory use
`examples/clipping/invisible-content-clipping-test.pdf` and write results to
`output/clipping/`.

- `01_clear_clipping.py` - remove clipping from a hidden image and a grouped
  set of vector paths so the content becomes visible again.
