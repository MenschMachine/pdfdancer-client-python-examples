- `.propagate-context.md` was referenced by `.propagate-prompt.md` but was missing from this checkout.
- The local API container image `ghcr.io/menschmachine/pdfdancer-api:pr-68` did not start with the prompt-provided information alone. It also required a valid `PDFDANCER_API_KEY_ENCRYPTION_SECRET` value with an AES key length of 16, 24, or 32 bytes.
- The same local API container expected a `./fonts` directory inside `/home/app`; without mounting one, startup failed with `Font directory does not exist or is not a directory: ./fonts`.
- The prompt said API schemas are available at `$API_URL:/swagger-ui`, but this environment did not export `API_URL`, and the local container returned `404 Not Found` for `/swagger-ui`.
- The fixture path in the prompt/capability note was `examples/clipping/invisible-content-clipping-test.pdf`, but in the public `MenschMachine/pdfdancer-examples` repository the source file currently lives at `clipping/invisible-content-clipping-test.pdf`.
